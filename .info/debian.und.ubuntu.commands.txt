sudo apt-get install
#установить
sudo nano
#открыть в текстовом редакторе
apt-get install subversion
#установить subversion
sudo dpkg-reconfigure console-setup
#переконфигурация консоли
sudo dpkg-reconfigure locales
#переконфигурация локалей
sudo apt-get install console-cyrillic
#установка кирилической консоли
sudo dpkg-reconfigure console-cyrillic
#переконфигурация кирилической консоли

#консоль
gksu gedit /etc/default/console-setup
sudo nano -w /etc/default/console-setup
sudo /etc/init.d/console-setup restart
sudo update-initramfs -u
apt-get -f install

sudo shutdown -h now
#выключить
sudo init 0
#выключить
sudo telinit 0
#выключить
sudo halt
#выключить
sudo poweroff
#выключить
sudo init 6
#перезагрузить
sudo shutdown -r now
#перезагрузить
sudo reboot
#перезагрузить
sudo shutdown -h hours:minutes &
#запланированое выключение
sudo shutdown -c
#отменить запланированное
sudo logout
#завершить сесию

sudo bklid 
#посмотреть ID монтированных разделов

mount -o -w -n /

gksu gedit

установить что-то
sudo apt-get install

#русификация
sudo apt-get install language-pack-gnome-ru-base language-pack-gnome-ru

#монтирование разделов
sudo su
fdisk -l

#ставим netbeans
sudo apt-get install sun-java6-bin sun-java6-jdk
sudo apt-get install netbeans
sudo apt-get remove netbeans
sudo sh netbeans-6.9.1-ml-php-linux.sh

#аналог icq
sudo apt-get install qutim

#конфигурация gnome
ALT+F2: gconf-editor

#ставим skype
sudo apt-get install skype

#Аналог Total Comander
gnome-commander

#Переместить значки слева направо
http://forum.ubuntu.ru/index.php?topic=102373.0
gconftool-2 --set "/apps/metacity/general/button_layout" --type string "menu:minimize,maximize,close"

#аналог PuntoSwitcher
sudo apt-get install gxneur

#меняем hostname
cat /etc/hostname
sudo nano /etc/hostname
sudo /etc/init.d/hostname
sudo /etc/init.d/hostname.sh
/etc/init.d/hostname start
sudo sed -i 's/debian/new-lenny/g' /etc/hosts
sudo /etc/init.d/networking restart